# MIC / Inner-Circle environment variables (example)
# Copy to .env and adjust values for your environment.

# Postgres connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mic
DB_USER=mic
DB_PASSWORD=micpass

# Redis (rate limiting)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Rate limiting
RATE_LIMIT_POSTS_PER_MINUTE=60

# Error handling (future)
# ERROR_DETAIL=public   # public|internal
